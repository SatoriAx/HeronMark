[package]
name = "heronmark"
version = "0.1.0"
description = "鹭印 HeronMark - 本地批量照片水印工具"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
